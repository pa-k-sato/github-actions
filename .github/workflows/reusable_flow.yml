name: Reusable flow

on:
  workflow_call:
    inputs:
      a-name:
        type: string

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
      - name: echo passed name
        run: |
          sleep 120 \
          echo "passed is ${{ inputs.a-name }}"
